import "./Title.css";

export default function Title(){
    return(
        <div className="title">
            <div className="titletitle">
                Tenzies
            </div>
            <div className="title-desc">
                Roll until all the dice are the same value. Click each die to freeze it at at its current value between rolls.
            </div>
        </div>
    );
}